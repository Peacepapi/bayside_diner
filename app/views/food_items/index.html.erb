<% @food_items.each do |food_item| %>
  <p><%= food_item.name %></p>
  <p><%= food_item.description %></p>
  <p><%= food_item.price %></p>
  <p><%= food_item.section %></p>
  <img src="<%= food_item.image_url %>" alt="<%= food_item.name %>" />
<% end %>
